version: 1
jobs:
  fail:
    steps:
      - name: 必败 step
        shell: cmd
        run: |
          exit 42
  after:
    needs: [fail]
    steps:
      - name: 不应执行
        shell: cmd
        run: |
          echo SHOULD NOT RUN
